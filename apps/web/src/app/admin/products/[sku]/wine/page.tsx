/** Admin: Edytor wine_details (wizualny CMS) — /admin/products/[sku]/wine */
export default function AdminWineEditorPage({
  params,
}: {
  params: Promise<{ sku: string }>;
}) {
  return <div>Edytor danych wina — UI w przygotowaniu</div>;
}
